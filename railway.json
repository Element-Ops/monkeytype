{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile --config.engine-strict=false && pnpm turbo run build --filter @monkeytype/frontend"
  },
  "deploy": {
    "startCommand": "npx -y serve -s frontend/dist -l ${PORT:-3000} --cors --no-clipboard"
  }
}
